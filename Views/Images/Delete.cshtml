@model EngsVirkeri.Models.Image

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Image</h4>
    <hr />
    
                
                    
        @*<div class="card-img" style="background-image: url('@Url.Content(context.Images.FirstOrDefault(i => i.Product.Id == item.Id).Path)')"></div>*@
        <div class="card-img" style="background-image: url('@Url.Content(Path)')"></div>

        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" asp-route-id="Id"/>
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            <a asp-action="Index">Back to List</a>
        </form>
                        
                    
               
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
